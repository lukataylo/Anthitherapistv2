/**
 * Mirror detail screen — re-exports the history detail view.
 *
 * This file makes the entry detail screen accessible under the /mirror/ path
 * after the navigation restructure. The underlying component is identical to
 * the original history/[id].tsx screen.
 */

export { default } from "../history/[id]";
